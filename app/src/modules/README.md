# `modules` Folder

Folder contains functionality that needs to be accessible to the entire application. `nav.py` is a module that supports custom navigation bar on the left of the app along with some basic Role-Based Access Control (RBAC).
